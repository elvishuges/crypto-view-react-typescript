on : push
name : 🚀 Implementar website em
jobs push :
   web-deploy :
     name : 🎉 Implementar
    runs-on : ubuntu-latest
    steps :
    - name : 🚚 Obter o código mais recente
      usa : actions/checkout@v2.3.2

    - nome : 📂 Os arquivos de sincronização
      usam : SamKirkland/FTP-Deploy-Action@4.0.0
      com :
         servidor : files.000webon: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.samkirkland.com
        username: myFtpUserName
        password: ${{ secrets.ftp_password }}host.com
        nome de usuário : sistemahuges
        senha : $ {{SSH_SECRET}}